import { createTheme } from '@mui/material'

export const theme = createTheme({
    palette: {
        primary: {
            main: '#bda734',
            light: '#f7f2ab'
        }
    },
    typography: {
        fontFamily: '"Nunito", sans-serif',
    }
})